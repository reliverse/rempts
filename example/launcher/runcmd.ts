/* import { runCmd } from "~/mod.js";

import { cmdMinimal } from "./app/cmds.js";

async function main() {
  try {
    // Run the command with test arguments
    await runCmd(await cmdMinimal(), ["--init=cmd1 cmd2"]);
  } catch (error) {
    console.error("Error running command:", error);
  }
}

await main();
 */
